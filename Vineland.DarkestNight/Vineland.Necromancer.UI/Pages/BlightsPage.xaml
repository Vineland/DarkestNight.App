<?xml version="1.0" encoding="UTF-8"?>
<ui:BasePage xmlns="http://xamarin.com/schemas/2014/forms" 
xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
x:Class="Vineland.Necromancer.UI.BlightsPage" 
xmlns:ui="clr-namespace:Vineland.Necromancer.UI" 
Title="Blights"
BackgroundImage="background">
	<ui:BasePage.Resources>
		<ResourceDictionary>
		<ui:IsSelectedConverter x:Key="isSelectedConverter" />
			</ResourceDictionary>
	</ui:BasePage.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
	<ListView ItemsSource="{ Binding Locations }" HasUnevenRows="true" >
		<ListView.ItemTemplate>
			<DataTemplate>
				<ui:LocationBlightsCell />
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
		<StackLayout Grid.Row="1" Orientation="Horizontal" HorizontalOptions="CenterAndExpand" Spacing="8">
				
				<Button Image="blight_move" Command="{Binding MoveCommand}"/>
				<Button Image="blight_remove" Command="{Binding DestroyCommand}"/>
				
			</StackLayout>
		</Grid>
</ui:BasePage>